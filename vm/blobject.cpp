#include "blobject.h"


bool BLTValue::EqualTo(BLTValue* other)
{
    if(type != other->type)
    {
        // if(LuaType::is_number((LuaType::Type)type) && LuaType::is_number(other->type))
        // {
        //     
        // }
    }

    return false;
}
